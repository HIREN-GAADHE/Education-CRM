# ============================================================
# EduERP — Production Environment Template
# ============================================================
# Copy this file to .env on Render and fill in real values.
# On Render: set these via the "Environment" tab in your service.
# ============================================================

# ── Application ─────────────────────────────────────────────
APP_NAME=EduERP
APP_VERSION=1.0.0
DEBUG=false
ENVIRONMENT=production
LOG_LEVEL=INFO

# ── Database (Neon PostgreSQL) ───────────────────────────────
# Get this from your Neon dashboard → Connection Details
# Must use the asyncpg driver for async SQLAlchemy
# Format: postgresql+asyncpg://user:password@host/dbname?sslmode=require
DATABASE_URL=postgresql+asyncpg://your_neon_user:your_password@ep-xxxx.us-east-2.aws.neon.tech/neondb?sslmode=require

# ── Security ─────────────────────────────────────────────────
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=REPLACE_WITH_GENERATED_64_CHAR_SECRET
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7
REFRESH_COOKIE_SECURE=true
REFRESH_COOKIE_SAMESITE=none

# ── CORS ─────────────────────────────────────────────────────
# Set to your Vercel app URL (no trailing slash)
CORS_ORIGINS=["https://your-app.vercel.app"]

# ── Server ───────────────────────────────────────────────────
# Render sets PORT automatically. WORKERS should be 1 for free tier.
HOST=0.0.0.0
PORT=8000
WORKERS=1

# ── Super Admin (only needed on first deploy / RUN_SEED=true) ─
SUPER_ADMIN_EMAIL=admin@your-domain.com
SUPER_ADMIN_PASSWORD=Change@Me@Right@Now

# ── Seed Control ─────────────────────────────────────────────
# Set to "true" only on the FIRST deploy to create the super admin.
# Set to "false" afterwards to prevent re-seeding.
RUN_SEED=false

# ── Optional: Storage (leave blank to use local disk) ────────
# STORAGE_BACKEND=s3
# S3_BUCKET_NAME=
# S3_ACCESS_KEY=
# S3_SECRET_KEY=
# S3_ENDPOINT_URL=
# S3_REGION=us-east-1

# ── Optional: Email ───────────────────────────────────────────
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=
# SMTP_FROM_EMAIL=noreply@your-domain.com
# SMTP_USE_TLS=true
